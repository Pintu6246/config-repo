server:
  port: 9004

spring:
  application:
    name: loan-service

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/loans_db
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
